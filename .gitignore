# --- Rime 自动生成及编译文件 (最占空间) ---
build/
*.bin
*.prism

# --- 用户数据 (会随打字不断变化，不应同步) ---
# 用户词频数据库
*.userdb/
# 同步文件夹
sync/
# 记录当前选中方案和部署状态的文件
user.yaml
# 记录安装 ID 的文件 (每个设备应该不同)
installation.yaml

# --- 临时及备份文件 ---
# 您刚才生成的备份
*.bak
# 常见的临时文件
*.swp
*~
.DS_Store
Thumbs.db

# --- 其他 (视情况而定) ---
# 如果您不需要同步 Go 语言的工作区配置 (go.work 在您的目录中存在)
go.work
go.sum
